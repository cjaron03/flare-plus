# core dependencies (use compatible versions to avoid pulling CUDA)
python-dateutil>=2.8.2
requests>=2.31.0
pandas>=2.0.0,<2.2.0
numpy==1.26.4
pytz>=2023.3

# data storage
psycopg2-binary>=2.9.9
sqlalchemy>=2.0.0

# ml framework (CPU-only versions, no CUDA/GPU dependencies)
scikit-learn>=1.3.0,<1.6.0
scipy==1.13.1
# xgboost installed separately from source to avoid cuda dependencies
lightgbm>=4.1.0,<5.0.0
imbalanced-learn>=0.11.0
# survival analysis
lifelines>=0.27.8

# experiment tracking
mlflow>=2.10.0

# api
flask>=3.0.0
flask-cors>=4.0.0
gunicorn>=21.2.0
fastapi>=0.109.0
uvicorn[standard]>=0.25.0

# utilities
pyyaml>=6.0
python-dotenv>=1.0.0
schedule>=1.2.0
tqdm>=4.66.0
